# Legacy Supabase artifacts

This directory is retained only as historical schema reference from M1–M3.
Runtime auth, database, and storage now use Appwrite Cloud Free. See
[`APPWRITE.md`](../APPWRITE.md).
